# comandos utilizados desde a criação do repositório até o envio ao GitHub
1. git init
2. git add README.md 
3. git commit -m 'descrição do primeiro commit'
4. git remote add origin git@github.com:flavio-silva/entregas-code-education.git
5. git push origin master
